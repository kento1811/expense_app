name: backend_api
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.5

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  shelf: ^1.4.1        # 1. Framework xử lý web server cực nhẹ
  shelf_router: ^1.1.4 # 2. Bộ điều phối đường dẫn (Routing)
  postgres: ^3.0.0 

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
